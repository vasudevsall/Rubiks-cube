{
  "name": "cube",
  "version": "1.0.0",
  "description": "A three js implementation of a solvable rubik's cube",
  "main": "index.html",
  "scripts": {
    "start": "npm run lite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite": "lite-server",
    "clean": "rimraf dist",
    "build": "npm run clean && copyfiles index.html js/* css/* node_modules/three/build/* node_modules/three/examples/jsm/controls/OrbitControls.js favicon.ico dist"
  },
  "author": "Vasu Dev Sall",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.3.0",
    "lite-server": "^2.5.4",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "three": "^0.118.3"
  }
}
